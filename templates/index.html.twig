{% extends 'base.html.twig' %}
{% block body %}
	<h1>La liste des utilisateurs</h1>
	{% if users %}
		<ul>
			{% for user in users %}
				<a href="{{ path('user_profile',{id : user.id}) }} ">
					<li>{{ user.name }}</li>
				</a>
			{% endfor %}
		</ul>
	{% endif %}


{% endblock %}
